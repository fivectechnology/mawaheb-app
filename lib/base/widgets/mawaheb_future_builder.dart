import 'package:flutter/material.dart';

class MawahebFutureBuilder extends StatelessWidget {
  const MawahebFutureBuilder({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
